import { Server } from './server';
import { RTCDataChannel, RTCPeerConnection } from 'werift';
export declare const maxMessageSize = 10000;
export declare class Connection {
    nethernet: Server;
    connectionId: bigint;
    rtcConnection: RTCPeerConnection;
    reliable: RTCDataChannel | null;
    unreliable: RTCDataChannel | null;
    promisedSegments: number;
    buf: Buffer | null;
    constructor(nethernet: Server, connectionId: bigint, rtcConnection: RTCPeerConnection);
    setChannels(reliable: RTCDataChannel | null, unreliable?: RTCDataChannel): void;
    handleMessage(data: string | Buffer): void;
    onPacket(packet: Buffer): void;
    write(data: string | Buffer): number;
    close(): void;
}
