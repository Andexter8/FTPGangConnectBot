import { XboxMessage } from '..';
import { MessageEntity } from './MessageEntity';
import { APIGroup } from '../rest';
export declare class Group extends MessageEntity {
    client: XboxMessage;
    id: string;
    name: string;
    ownerId: string;
    conversationId: string;
    constructor(client: XboxMessage, data: APIGroup);
    get owner(): import("./User").User | undefined;
    get conversation(): import("..").Conversation | undefined;
}
