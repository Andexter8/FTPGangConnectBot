import { XboxMessage } from '..';
import { MessageEntity } from './MessageEntity';
export type UserData = {
    id: string;
    gamertag?: string;
    displayImageURL?: string;
    colour?: {
        primary: string;
        secondary: string;
        tertiary: string;
    };
};
export declare class User extends MessageEntity {
    client: XboxMessage;
    id: string;
    gamertag?: string;
    displayImageURL?: string;
    colour?: {
        primary: string;
        secondary: string;
        tertiary: string;
    };
    constructor(client: XboxMessage, data: UserData);
    get conversation(): import("..").Conversation | undefined;
    fetch(force?: boolean): Promise<User>;
    _clone(): User;
    _patch(data: UserData): void;
}
