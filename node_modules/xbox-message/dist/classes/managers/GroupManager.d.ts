import { Collection } from '@discordjs/collection';
import { XboxMessage } from '../..';
import { Group } from '../Group';
import { APIGroup } from '../../rest';
export declare class GroupManager {
    client: XboxMessage;
    cache: Collection<string, Group>;
    constructor(client: XboxMessage);
    fetch(id: string): Promise<Group>;
    add(data: APIGroup, cache?: boolean, { id }?: {
        id?: string;
    }): Group;
}
