"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ConversationManager = void 0;
const collection_1 = require("@discordjs/collection");
const __1 = require("../..");
const GroupConversation_1 = require("../Conversation/GroupConversation");
const OneToOneConversation_1 = require("../Conversation/OneToOneConversation");
const XboxMessageError_1 = require("../errors/XboxMessageError");
class ConversationManager {
    constructor(client) {
        this.cache = new collection_1.Collection();
        this.client = client;
    }
    fetch(id, type) {
        return __awaiter(this, void 0, void 0, function* () {
            const existing = this.cache.get(id);
            if (existing)
                return existing;
            const data = yield this.client.rest.getConversation(type, id);
            if (!data)
                return null;
            return this.add(data, true);
        });
    }
    add(data, cache = true, { id } = {}) {
        const existing = this.cache.get(id !== null && id !== void 0 ? id : data.conversationId);
        if (existing) {
            return existing;
        }
        let entry;
        if (data.type === __1.ConversationType.OneToOne) {
            entry = new OneToOneConversation_1.OneToOneConversation(this.client, data);
        }
        else if (data.type === __1.ConversationType.Group) {
            entry = new GroupConversation_1.GroupConversation(this.client, data);
        }
        if (!entry)
            throw new XboxMessageError_1.XboxMessageError(XboxMessageError_1.XboxMessageErrorCodes.InvalidConversationType, data.type);
        if (cache)
            this.cache.set(id !== null && id !== void 0 ? id : entry.id, entry);
        return entry;
    }
}
exports.ConversationManager = ConversationManager;
