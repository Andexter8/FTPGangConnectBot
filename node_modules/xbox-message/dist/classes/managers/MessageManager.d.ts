import { Collection } from '@discordjs/collection';
import { Conversation, XboxMessage } from '../..';
import { Message } from '../Message';
import { APIMessage } from '../../rest';
export declare class MessageManager {
    client: XboxMessage;
    conversation: Conversation;
    cache: Collection<string, Message>;
    constructor(conversation: Conversation);
    fetch(id: string): Promise<Message>;
    fetch(): Promise<Collection<string, Message>>;
    private fetchMany;
    private fetchSingle;
    add(data: APIMessage, cache?: boolean, { id }?: {
        id?: string;
    }): Message;
}
